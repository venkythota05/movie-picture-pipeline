services:
  backend:
    build:
      context: ./starter/backend
    ports:
      - "5000:5000"

  frontend:
    build:
      context: ./starter/frontend
      args:
        REACT_APP_MOVIE_API_URL: http://localhost:5000
    ports:
      - "3000:3000"
    depends_on:
      - backend
